import Header from './header';
import Overlay from './overlay';
import Preloader from './preloader';
import Button from './button';

export const COMPONENTS = { Header, Overlay, Preloader, Button };

export default COMPONENTS;